name: yaru_widgets_example
description: Demonstrate yaru_widgets
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  handy_window: ^0.1.2
  provider: ^6.0.2
  yaru: ^0.3.2
  yaru_icons: ^0.2.1
  yaru_widgets:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/