name: example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  handy_window: ^0.3.1
  provider: ^6.0.3
  yaru: ^1.1.0
  yaru_colors: ^0.1.7
  yaru_icons:
    path: ../
  yaru_widgets: ^3.2.0

dependency_overrides:
  yaru_icons:
    path: ../

dev_dependencies:
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
