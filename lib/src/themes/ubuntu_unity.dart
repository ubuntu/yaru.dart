import '../../theme.dart';

const _primaryColor = YaruColors.ubuntuUnityPurple;

final yaruUbuntuUnityLight = createYaruLightTheme(
  primaryColor: _primaryColor,
);

final yaruUbuntuUnityDark = createYaruDarkTheme(
  primaryColor: _primaryColor,
);
